[build-system]
requires = ["maturin>=0.14,<0.15"]
build-backend = "maturin"

[project]
name = "nexsys"
dependencies = ["cffi"]

[tool.maturin]
python-source = "python"
bindings = "cffi"
compatibility = "linux"